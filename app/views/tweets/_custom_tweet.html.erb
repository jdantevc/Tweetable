<%= form_with(model: tweet, url: tweets_path, class: 'new-tweet-form') do |form| %>
  <%= form.text_area :body, placeholder: 'Write your tweet...', rows: 3, required: true %>
  <%= form.submit "Tweet", class: 'button button--primary' %>
<% end %>
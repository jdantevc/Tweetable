<%= form_with(model: tweet, url: tweet_path(tweet), method: :patch, class: 'new-tweet-form') do |form| %>
  <%= form.text_area :body, placeholder: 'Write your tweet...', rows: 3, required: true %>
  <%= form.submit "Update", class: 'button button--primary' %>
<% end %>